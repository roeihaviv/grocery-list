<li id="<%= dom_id item, :form %>" class="list-group-item">
  <%= form_with(model: item, local: false) do |form| %>
    <div class="mb-2">
      <%= form.label :item, class: "visually-hidden" %>
      <%= form.text_field :item, class: "form-control" %>
    </div>

      <div>
      <div class="d-grid">
        <%= form.submit class: "btn btn-outline-primary" %>
      </div>
    </div>
  <% end %>

</li>
